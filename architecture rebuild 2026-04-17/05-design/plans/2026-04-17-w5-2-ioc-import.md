# W5.2 — IOC system import

**Date:** 2026-04-17
**Status:** 🟢 Done (import executed; optional embedding deferred)
**Backlog:** `06-handoffs/backlog.md` W5.2

## Goal

Get Edmund's canonical IOC (IP → Offers → Content) framework spec into the `public.reference_docs` table so agents — Axel in particular — can cite it directly.

## What was imported

One row. Canonical entry-point directive from the retired IOC System v2.0 archive:

- **Source file:** `/Users/edmundmitchell/factory/reference/archive/ioc-system-v2/IOC_DIRECTIVE.md`
- **Source size:** 250 lines, ~10.8 KB
- **Source dated:** 2026-02-28 (v2.0+ revision)
- **reference_docs row:** id `df1b6b54-e722-47ec-a420-970a4d7f4b93`, slug `ioc-system-v2`, kind `framework`, version `1`

### Why this file (and not another)

The archive bundles 48 files. `IOC_DIRECTIVE.md` is **the** agent-facing directive — terminology (Season/Sprint/Checkpoint, Series Bucket, CTA Choreography), canonical ID format, 5-table Supabase schema, 10 behavioral rules, and the 9-step sprint lifecycle with A–H approval gates. The archive's own `ARCHIVE_NOTE.md` flags it first as "the agent entry point with terminology + schema." Everything else in the bundle is supporting material (SOPs, DDL, audit). For a single reference_doc row that Axel can quote, this is the payload Edmund wants.

## Candidates considered

| Path | One-liner | Why not |
|---|---|---|
| `reference/archive/ioc-system-v2/IOC_DIRECTIVE.md` | IOC-specific agent entry point | **PICKED** — the canonical agent-facing spec |
| `reference/archive/ioc-system-v2/IOC_System_v2.0_FileIndex.md` | Master index of all 48 files | Index, not the spec; pointers only |
| `reference/archive/ioc-system-v2/IOC-Audit-2026-02-28.md` | Systems audit that drove v2.0 revisions | Historical diagnostic, not the live framework |
| `reference/archive/ioc-system-v2/GEMINI.md` | Universal agent constitution | Not IOC-specific; general operating principles |
| `reference/archive/ioc-system-v2/directives/` (40 A/N/E/B/O/S files) | The actual SOPs referenced by the directive | Too granular for one `reference_docs` row; can be imported piecemeal later as individual Skills or reference_doc siblings |
| `reference/archive/ioc-system-v2/README_v02_REVISIONS.txt` | v1 → v2 changelog | Historical |
| `/Users/edmundmitchell/Library/Mobile Documents/com~apple~CloudDocs/CEO Cowork/` | CEO Cowork tree | Searched — no competing IOC doc. Only downstream references (Axel agent files, EM brand docs) which all tie back to the archived IOC system |
| `/Users/edmundmitchell/factory/architecture rebuild 2026-04-17/` | Current rebuild tree | Mentions IOC in backlog/audit/brainstorm notes but no new canonical spec |

No newer canonical IOC exists — the 2026-04-17 rebuild is a retirement of GravityClaw and the old IOC system lineage, not a rewrite of IOC itself. This archived directive is the last-known-good spec.

## Schema fit

`reference_docs` (from migration `009_create_reference_docs.sql`):

| Column | Used |
|---|---|
| `slug` | `ioc-system-v2` |
| `title` | `IOC System v2.0 — Agent Directive` |
| `body` | Full 250-line markdown, verbatim |
| `kind` | `framework` (one of the allowed enum values) |
| `version` | `1` (first import) |
| `metadata` | `{source_path, source_version, source_date, imported_at, imported_by, project: 'em-personal', related_candidates[], notes}` |

Note: the task prompt referenced `content` / `project` columns. The real schema has `body` (for the markdown) and no top-level `project` column. Project is recorded inside `metadata.project = "em-personal"`, which matches existing rebuild conventions.

## What was NOT done (deferred)

- **Optional embedding into `public.memory`.** The task description called this out as optional and said to skip if the OpenAI key wasn't locally available. The embedding path for this repo is Edge-Function-only (uses function secrets for `OPENAI_API_KEY`); I don't have a direct local OpenAI key and this agent shouldn't be minting embeddings out-of-band. **Flagging for follow-up:** deploy-or-invoke the embedding Edge Function against `reference_docs.id = df1b6b54-e722-47ec-a420-970a4d7f4b93` so Axel can also retrieve it via semantic search on `memory (namespace='knowledge', source='reference_doc', source_id=<id>)`.
- **Importing the 40 supporting A/N/E/B/O/S SOPs.** Out of scope for W5.2 (which is "one canonical spec"). If Edmund wants these available too, they fit better as individual `skill_versions` rows or as a separate batch of `reference_docs` with kind='playbook' — worth a dedicated backlog item.

## Verification

```sql
SELECT id, slug, title, kind, version, length(body) AS body_len, metadata->>'project' AS project
FROM public.reference_docs
WHERE slug = 'ioc-system-v2';
-- → df1b6b54-...  ioc-system-v2  IOC System v2.0 — Agent Directive  framework  1  10804  em-personal
```

## Next steps for Edmund

1. Eyeball the body rendering in the dashboard / via SQL to confirm markdown came through clean.
2. Point Axel at it — his system prompt/tool allowlist already lists `reference_docs` as readable (per W5.1).
3. If desired, trigger the embedding follow-up (see "What was NOT done").
4. Decide whether any of the 40 A/N/E/B/O/S directive files (SOPs) should also land in `reference_docs` or `skill_versions` — would be a new backlog item.
