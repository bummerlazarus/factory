# W9.2 follow-up — merges + function capture + noise cleanup

**Date:** 2026-04-19
**Epic:** W9.2 follow-ups (from run log)
**Status:** 🟢 DONE

## What shipped

1. **Edmund toggled** leaked-password protection in Supabase Dashboard → Authentication → Attack Protection. Advisor no longer reports `auth_leaked_password_protection`.
2. **Merged** `feat/oa-4-tsconfig` + `feat/w9-2-security-hardening` to `main` in dashboard repo, pushed to `origin/main` (merge commits `b1342b2`, `2615061`).
3. **Captured 3 historical functions** into a migration: `log_factory_event`, `dc_set_updated_at`, `update_vault_files_updated_at` (all with `SET search_path = pg_catalog, public`). Idempotent `CREATE OR REPLACE`; matches live definition from `pg_get_functiondef`.
4. **Added `service_role_all`** to 6 RLS-enabled-no-policy tables: `agent_conversations`, `agent_memory`, `agent_scratchpad`, `agent_tasks`, `slack_messages`, `workspace_items`. Zero behavior change (service_role bypasses RLS); clears advisor noise.
5. Branch `feat/w9-2-cleanup` merged (commit `02649ba`) and deleted locally + remote.

## Migrations applied

| # | Name | Effect |
|---|---|---|
| 1 | `20260419110000_capture_historical_functions` | `CREATE OR REPLACE` for 3 functions, matching live |
| 2 | `20260419110100_policies_rls_no_policy_tables` | `service_role_all` policy on 6 tables |

## Verification

`get_advisors(security)` returned 9 lints, all pre-existing + explicitly out-of-scope per W9.2 plan:

- 1× `security_definer_view` ERROR — `content_metrics_unified`
- 2× `extension_in_public` WARN — `pg_net`, `vector`
- 6× `rls_policy_always_true` WARN — 4 public form tables (Q10 says leave), `observations.observations_authenticated_update` (pre-existing), `vault_files.service_role_full_access` (pre-existing pattern)

**All W9.2-targeted lints are now clean**, including the INFO-level noise.

## Files touched (main)

- `dashboard/supabase/migrations/20260419110000_capture_historical_functions.sql` (new)
- `dashboard/supabase/migrations/20260419110100_policies_rls_no_policy_tables.sql` (new)

## W9.5 status

Now fully unblocked on the technical side:
- ✅ `npm run build` green (OA.4)
- ✅ Q10 security hardening complete (W9.2)
- ✅ Leaked-password toggled
- ✅ No outstanding advisor lints for shipping policies

**Still gated on:** Edmund's explicit green-light for prod deploy (per autonomy charter hard stop: "Production deploy / promotion to the edmund.dev-class domain").

## Cost

Negligible. ~$0.01 — a couple MCP calls + one Bash-heavy merge sequence.

## What's next

- **W9.5** — Vercel prod promotion. Waiting on Edmund's explicit "go" before proceeding.
- **W1.4** — dual-read parity report, available 2026-04-24 (7 days post W1.2).
- **Out-of-scope but flagged:** `content_metrics_unified` SECURITY DEFINER view (ERROR), `pg_net`/`vector` extensions in public schema, 5 remaining `rls_policy_always_true` warnings on observations/vault_files. All pre-W9.2, not blocking.
