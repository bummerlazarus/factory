# Autonomous run — W5.9 "Educated bets" Skill

**Date:** 2026-04-17
**Status:** 🟢 SHIPPED
**Plan:** [`05-design/plans/2026-04-17-w5-9-educated-bets.md`](../../05-design/plans/2026-04-17-w5-9-educated-bets.md)
**Predecessor:** W5.8 Double-down (commit `7603c97`)

## What shipped

- Migration `/Users/edmundmitchell/factory/dashboard/supabase/migrations/20260417100000_educated_bets_skill.sql`
- Applied to Supabase `obizmgugsqirmnjpirnh` via `apply_migration` (name `20260417100000_educated_bets_skill`).
- Function `public.educated_bets_scan(lookback_days int DEFAULT 30, top_n int DEFAULT 3) RETURNS jsonb`.
- pg_cron job `educated-bets-weekly`, schedule `0 9 * * 1` (Monday 09:00 UTC).
- Extended `reference_docs_kind_check` to include `'educated-bet'` (preserves W5.8's `'promotion'`).

## Verification

### 1. Live run

```sql
SELECT public.educated_bets_scan(30, 3);
```

```json
{
  "top_n": 3,
  "bet_ids": [
    "31e80b36-7e22-4fed-a669-bfd93998a987",
    "a268a43d-4781-451b-a639-4517f0806ba7"
  ],
  "proposed": 2,
  "processed": 2,
  "window_end": "2026-04-17T21:59:10.257899+00:00",
  "window_start": "2026-03-18T21:59:10.257899+00:00",
  "lookback_days": 30,
  "skipped_existing": 0
}
```

Note: only 2 of 3 templates materialized. The `platform-cross-post` template
was skipped because there are no Beehiiv rows in `content_metrics_unified`
yet — the cross-platform anchor subquery returns empty. This is correct
graceful degradation per the plan.

### 2. Idempotency re-run

```sql
SELECT public.educated_bets_scan(30, 3);
```

```json
{
  "top_n": 3,
  "bet_ids": [],
  "proposed": 0,
  "processed": 2,
  "window_end": "2026-04-17T21:59:16.098861+00:00",
  "window_start": "2026-03-18T21:59:16.098861+00:00",
  "lookback_days": 30,
  "skipped_existing": 2
}
```

### 3. Sample bet row

```
slug:  bet-2026-04-17-aef0083123
title: Educated bet: Because "How to save time in catholic ministry" landed on
       551 view_count over the last 30 days, try a variant with the same angle
       but a sharper hook.

body (head):
  Auto-generated by Educated-bets Skill (W5.9).

  **Template:** double-hook-variant
  **Window:** last 30 days
  **Proposed:** 2026-04-17 21:59 UTC

  ## Evidence
  - **top-performer**: How to save time in catholic ministry — view_count = 551

  ## Angle
  ...
```

Second bet (framework-deepen, keyword fallback because all YouTube tags are
empty/null right now):

```
slug:  bet-2026-04-17-f692c5a306
title: Educated bet: Because "grow" keeps surfacing in top-performing titles
       (1x in the last 30 days), propose a framework essay on grow.
```

### 4. Migration idempotency

Re-applied the guarded DDL (extension + constraint) as
`20260417100000_educated_bets_skill_reapply_check` — `{"success": true}`,
no-op. Function uses `CREATE OR REPLACE`; schedule uses `unschedule + schedule`
in a guarded `DO` block.

### 5. Cron registered

```
jobname:  educated-bets-weekly
schedule: 0 9 * * 1
command:   SELECT public.educated_bets_scan(30, 3);
```

### 6. Test cleanup

```sql
DELETE FROM public.reference_docs
WHERE kind='educated-bet' AND metadata->>'source'='educated_bets';
-- RETURNING: bet-2026-04-17-aef0083123, bet-2026-04-17-f692c5a306
```

Confirmed: 2 rows removed. `reference_docs` kind='educated-bet' is now empty.
Next scheduled run (Monday 2026-04-20 09:00 UTC) will land fresh rows.

## Data observations

Current snapshot state is thin:

- `content_metrics_unified` in the 30d window: 8 rows total, 2 distinct
  YouTube videos, 0 Beehiiv posts (publication only has drafts).
- YouTube `tags` are empty (`[]`) / `NULL` on both videos — the `_top_tags`
  CTE returns zero rows. Function falls through to title-keyword extraction,
  which is why the framework-deepen bet cites "grow" instead of a tag.

When Beehiiv gets a live post and YouTube backfills tags, next week's run
will also emit the `platform-cross-post` bet and richer framework bets.

## Files touched

- `dashboard/supabase/migrations/20260417100000_educated_bets_skill.sql` (new)
- `architecture rebuild 2026-04-17/05-design/plans/2026-04-17-w5-9-educated-bets.md` (new)
- `architecture rebuild 2026-04-17/06-handoffs/autonomous-runs/2026-04-17-w5-9-educated-bets.md` (this file)
- `architecture rebuild 2026-04-17/06-handoffs/backlog.md` (W5.9 → 🟢)

## Follow-ups

- Backfill YouTube `agent_youtube_videos.tags` so the tag-based framework-deepen branch lights up.
- Once Beehiiv starts sending (publication has only drafts today), the `platform-cross-post` template will begin firing.
- Consider a 4th template once observation kinds support "question" rows ("audience-ask mirror").
- Split `educated-bet` into a dedicated ephemeral table when the inbox fully moves to Supabase — same follow-up as W5.8's `promotion` kind.
