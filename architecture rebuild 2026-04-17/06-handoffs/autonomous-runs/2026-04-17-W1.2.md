# Autonomous run log — W1.2

**Epic:** W1.2 — Phase 2.5: rewire semantic-search callers to `match_memory()` + dual-read scaffolding
**Plan:** inline
**Status:** 🟢 DONE (convention documented; codebase audit empty — zero live callers to rewire)
**Done:** 2026-04-17

## Finding

The "rewire callers" epic turned out to be essentially a no-op in the factory codebase.

```
$ grep -i 'pinecone' /Users/edmundmitchell/factory/dashboard  # recursive
  (no matches)
$ grep 'search_memory\|ask_priestley\|ask_hormozi\|ask_channel' \
  /Users/edmundmitchell/factory/{dashboard,skills,agents,supabase,ops}/**
  (only the retiring migration script)
```

- **Dashboard:** no Pinecone or memory search. `/inbox/promotions` queries `skill_versions`, not memory.
- **Skills:** `/skills/session-retro/`, `/skills/voice-tone/`, `/skills/daily-recap/` — no semantic search.
- **Agents:** `agents/cordis/` identity + CLAUDE.md — references `search_memory` conceptually but doesn't execute it (Cordis calls tools; tool implementations live in GravityClaw).
- **Supabase Edge Functions:** `capture/` writes to memory, doesn't read.
- **ops/scripts:** migration script only; one-shot.

Historical callers — `search_memory`, `ask_channel`, `ask_priestley`, `ask_hormozi` — are all in `/Users/edmundmitchell/gravityclaw/`, which the charter marks read-only. They stay there until GravityClaw itself retires (W9.4).

**Conclusion:** no migration work; the convention just needs to be documented so future callers land on the right pattern.

## Files touched

- `supabase/README.md` — added "Semantic search convention" section: `match_memory()` RPC is the canonical entry point; use `dualread.ts` when parity logging is wanted; audit snapshot.

## Supabase artifacts

None.

## Subagents dispatched

None.

## Verification

### Audit
```
$ grep -l -i 'pinecone' /Users/edmundmitchell/factory/dashboard
(empty)

$ grep -l -i 'pinecone' /Users/edmundmitchell/factory/skills /Users/edmundmitchell/factory/agents /Users/edmundmitchell/factory/supabase/functions
/Users/edmundmitchell/factory/supabase/functions/_shared/dualread.ts   ← intentional (parity shadow reads)
```

### match_memory() RPC alive
```sql
SELECT * FROM public.match_memory(
  array_fill(0.0::real, ARRAY[1536])::vector,
  'knowledge',
  5,
  '{}'::jsonb
);
→ 0 rows on zero-vector query (expected); no error; RPC compiled and callable.
```
(Previously verified in Phase 2 execution log.)

### Dual-read helper callable
- `deno check functions/_shared/dualread.ts` clean.
- Depends on `public.memory_dualread_log` (migration 015, applied).

## Decisions made

- **Convention over rewrite.** No code migration. The payoff of W1.2 is a documented convention — future callers (W2.2 `/inbox`, W6.x research-director Skills, Cordis replacements) build on `match_memory()` from day one.
- **Do not port GravityClaw callers.** Per charter, `/Users/edmundmitchell/gravityclaw/` is read-only. Those callers go away with W9.4 decommission, not via piecewise rewire.

## Follow-ups flagged

- **Real parity data only lands when a non-write caller exists.** `memory_dualread_log` will stay empty until the first read caller (likely a Skill) lands. W1.4 (1-week parity observation window) is gated on that — not on calendar time.
- **If a near-term Skill needs Pinecone-compatible results to avoid regressions during cutover**, wire it through `dualread.ts` from the start; otherwise, `match_memory()` is cleaner.

## Cost

None.

## What's next

1. Close W1.1 once migration attempt #6 finishes and the 12,585 → 12,785 gap is closed.
2. Pick next epic — W2.2 (`/inbox` captures page) has the highest downstream leverage now that Wave 2 write-path is fully deployed. W2.3 (webhook doc) is a short companion.
