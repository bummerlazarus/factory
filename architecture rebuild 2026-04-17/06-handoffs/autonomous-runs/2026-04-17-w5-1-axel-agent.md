# Run log — W5.1 Axel agent scaffold

**Date:** 2026-04-17
**Epic:** W5.1 — Axel agent: system prompt + tool allowlist. Scope: ideation, drafting, repurposing. Reads `reference_docs`, `skill_versions` for voice-tone + framework Skills.
**Status:** 🟢 DONE

## Files written

| File | Purpose |
|---|---|
| `COWORK_PATH/Agent Personalities/agents/axel/identity.md` | Axel's character, voice, speaking style, "what Axel is NOT" (41 lines) |
| `COWORK_PATH/Agent Personalities/agents/axel/CLAUDE.md` | Active agent context: remit, reference docs, working principles, workflow, hand-off rules, tool allowlist, routing table (120 lines) |

**Combined: 161 lines** (well under the <400 line constraint).

Absolute paths:

- `/Users/edmundmitchell/Library/Mobile Documents/com~apple~CloudDocs/CEO Cowork/Agent Personalities/agents/axel/identity.md`
- `/Users/edmundmitchell/Library/Mobile Documents/com~apple~CloudDocs/CEO Cowork/Agent Personalities/agents/axel/CLAUDE.md`

No soul.md — per brief, v1 skips it.

## Folder location — a naming-clash note

`COWORK_PATH/Agent Personalities/agents/` contains **role-named** folders (`content/`, `developer/`, `ceo/`, etc.) — plus one personality-named folder as the exception: `cordis/`. Inside those role folders, different agent personas are assigned:

- `content/` → **Corva** (content strategist/producer, in the old schema)
- `developer/` → **Axel** (technical implementation, in the old schema — emoji ⚙️, color green #22c55e)

The architecture rebuild's six-agent troupe (see `05-design/finished-state.md`) **reassigns** names:

- **Axel** = content engine / factory lead (ideation, drafting, repurposing)
- **Corva** = meta / session retro / promotion drafter
- **Lev** = ingest / transcription
- **Hild** = ops / client work

That means "Axel" in the rebuild is a different agent from "Axel" in the old `agents/developer/` folder. Rather than rename that folder (instructions said "Don't touch other agents' files"), I followed the brief's explicit guidance: "If there's already a `cordis/`, `corva/`, etc. under `COWORK_PATH/Agent Personalities/agents/`, put `axel/` alongside." — i.e. use the `cordis/` personality-named-folder exception pattern.

**Result:** `agents/axel/` created as a personality-named folder next to `agents/cordis/`. The old `agents/developer/` folder (containing old-Axel-the-developer) is left untouched. Routing tables in other agents still point to `agents/developer/` for code/infra work — no cross-edits required. When Edmund is ready to retire the old `developer/` identity or consolidate naming, that's a separate cleanup epic.

**One-line reason for chosen location:** follow the brief's instruction and `cordis/`'s precedent for personality-named folders; avoid editing sibling agents' files.

## Taste calls — documented

### Emoji: 🛠️ (hammer-and-wrench)

- Signals builder/factory energy — matches Axel's "Factory Lead" title.
- No collision with existing agents: 🤖 (Cordis) · 📊 (Tokamak) · 📣 (Lev-marketing) · ✍️ (Corva-content) · 🔍 (Feynman) · ⚙️ (old Axel-developer) · 🎨 (Hild) · 🗂️ (Kardia).
- Distinct from ⚙️ (old Axel-dev, single-gear) — 🛠️ is recognizably a different glyph.

### Accent Color: Sky #0ea5e9

- Bright, energetic, suggests clarity and craft.
- No collision: Cordis indigo #6366f1 · Tokamak slate #475569 · Lev amber #f59e0b · Corva purple #a855f7 · Feynman teal #14b8a6 · Axel-dev green #22c55e · Hild coral #f97316 · Kardia rose #f43f5e.
- Sky blue is visibly distinct from the indigo and teal already in use.

### Role title: "Factory Lead — content ideation, drafting & repurposing"

- Crisp, maps to the backlog language ("ideation, drafting, repurposing") and to the rebuild's "department: Factory" assignment.
- Avoids "Content Lead" to keep bright-line separation from Corva's content-strategy-and-retro remit.

## Tool allowlist — summary

Documented in `CLAUDE.md` Tool Allowlist section. Highlights:

- **Read:** `reference_docs`, `skill_versions`, `memory` (semantic search), `work_log`, `sessions`, content-engine template archive.
- **Write:** `workspace/`, `work_log` insert, `observations` insert, `reference_docs` insert for durable artifacts only.
- **MCP:** `mcp__capture-mcp__capture` explicitly included.
- **Explicitly denied:** publishing tools, deploy tools, `skill_versions` approvals, filesystem writes outside sandbox, sending external messages on Edmund's behalf.

## Working principles cited, not invented

Per brief ("Don't invent Voice Discovery content — cite it"):

- Voice doc lives at `COWORK_PATH/brands/em/02 - brand/voice_tone_guide.md` (found; canonical, owner=EM, last updated 2026-03-08).
- `CLAUDE.md` points Axel at that file explicitly and names the specific principles ("testimony over teaching," "unexpected combinations of thinkers," "liturgical cadence at emotional peaks," "framework-forward," "raw beats polished") as defaults — with a hard instruction to consult the source file before drafting rather than paraphrase rules inline.
- A `feedback_voice_discovery.md` memory file at the path in the brief (`/Users/edmundmitchell/.claude/projects/-Users-edmundmitchell-gravityclaw/memory/feedback_voice_discovery.md`) does **not** exist in the current repo state. The canonical source is the voice guide. No invention — Axel reads it at runtime.

## Routing table — included in CLAUDE.md

Keywords covered (per brief): `draft`, `write`, `essay`, `outline`, `hook`, `opener`, `script`, `show notes`, `chapter markers`, `newsletter`, `cold open`, `subject line`, `repurpose`, `carousel`, `shorts`, `cuts`, `caption`, `reel`, `thread`. Each keyword paired with an example phrasing.

Hand-off rules documented (when to route to Cordis, Corva, Lev, Hild, research-director, old developer agent).

## Verification

- Files written: confirmed via `wc -l` — 41 + 120 = 161 lines combined.
- No edits to sibling agents' folders — confirmed (only `axel/` is new; `cordis/`, `content/`, `developer/`, etc. untouched).
- No code changes in `dashboard/`, no migrations, no Edge Function deploys — confirmed.
- No runtime registration step — confirmed; dashboard reads agents from disk.
- Backlog updated (see below).

## Decisions made this run

1. **Folder naming** — used personality-named `axel/` alongside `cordis/` (not a new role folder), per brief. Old `developer/` folder left untouched.
2. **No soul.md** — per brief.
3. **Tool allowlist scope** — explicit allow + explicit deny list in `CLAUDE.md`, rather than leaving implicit. Makes the "doesn't need dangerous file tools" constraint enforceable by whoever wires the runtime later.
4. **Voice principles cited by reference** — the CLAUDE.md lists the principle names as defaults but instructs Axel to read `voice_tone_guide.md` for the actual rules. No invented Voice Discovery content.

## Ambiguity Edmund should resolve

1. **Old Axel-the-developer in `agents/developer/`.** The old identity still exists on disk (emoji ⚙️, color green #22c55e). In the rebuild, the developer role no longer has "Axel" assigned to it. This is not breaking anything right now (no runtime collision), but it's a naming debt. When you're ready, options are: (a) rename the persona inside `agents/developer/` to something else, (b) retire that agent entirely since the rebuild doesn't list a dedicated developer agent in the six-agent troupe, (c) leave it and accept the drift. Not blocking W5.1 — flagging for future cleanup.
2. **Workspace path for drafts.** `CLAUDE.md` tells Axel to drop drafts in `workspace/`. The rebuild's conventions don't yet pin this down — could mean a `workspace/` folder inside `agents/axel/`, or a dashboard-level `workspace/` surface (e.g. `dashboard/workspace/`), or a Supabase table later. Defaulted to generic `workspace/` and deferred the pin until Edmund picks a home for drafts (likely W5.2 IOC system import or a follow-up).
3. **`mcp__capture-mcp__capture` naming.** The brief used that exact name. The deployed function is `capture-mcp` and the tool it exposes is named `capture`. In Claude Desktop/Code the tool surfaces as `mcp__<server-name>__capture` where the server name is whatever Edmund configured it as locally. The CLAUDE.md uses the brief's exact string — if Edmund's MCP server is named differently, that reference needs a one-line update to match.

## Cost

Negligible. No LLM/embedding calls. Pure file writes.

## What's next

Wave 5 continues:

- **W5.2** — IOC system import (content-pipeline canonical spec → `reference_docs` or `/skills/content-pipeline/`). This would slot naturally next to Axel — it's the playbook he'd read from.
- **W5.3** — YouTube metrics ingest.
- **W5.4** — Instagram metrics ingest.

Proceeding to whichever epic Edmund picks next.
