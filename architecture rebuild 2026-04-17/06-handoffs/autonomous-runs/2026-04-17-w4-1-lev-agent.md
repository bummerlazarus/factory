# Run log — W4.1 Lev agent scaffold

**Date:** 2026-04-17
**Epic:** W4.1 — Lev agent: system prompt + tool allowlist (match Cordis pattern). `/agents/lev/identity.md` + `CLAUDE.md`. Scope: voice + ingest.
**Status:** 🟢 DONE
**Pattern reference:** `/Users/edmundmitchell/factory/agents/cordis/` (Phase 3 MVP)

## Files touched

| File | Change |
|---|---|
| `/Users/edmundmitchell/factory/agents/lev/identity.md` | **New** — Name/Role/Emoji/Accent-Color frontmatter + character/remit/NOT-sections, ~40 lines. |
| `/Users/edmundmitchell/factory/agents/lev/CLAUDE.md` | **New** — System prompt + explicit tool allowlist + voice-memo path + ingest orchestration + emergent-theme observations rule + routing table, ~100 lines. |
| `/Users/edmundmitchell/factory/architecture rebuild 2026-04-17/06-handoffs/backlog.md` | W4.1 status ⚪ → 🟢 with one-line note. |

Total: ~160 lines across identity.md + CLAUDE.md, well under the 400-line budget.

## Directory choice

Put the Lev folder at `/Users/edmundmitchell/factory/agents/lev/` — the same tree Cordis already uses in-repo. The backlog entry literally says `/agents/lev/identity.md`, and there is exactly one other in-repo agent (`/agents/cordis/`) already. The cowork path (`COWORK_PATH/Agent Personalities/agents/`) has an unrelated older "Lev" identity (Marketing / brand strategist, amber) — intentionally not touched. The rebuild notebook's Lev is the ingest role; this folder is where the rebuild-side canonical version lives.

**Note for Edmund:** There are now two "Lev" identities in the system:
- `/Users/edmundmitchell/factory/agents/lev/` (this one — ingest conductor, cyan, 🎙️) — the rebuild canonical.
- `COWORK_PATH/Agent Personalities/agents/marketing/` (older — brand strategist, amber, 📣) — untouched per constraints. If Edmund wants to either rename the older one or archive it, flag it as a follow-up.

## Taste calls

| Choice | Pick | Reason |
|---|---|---|
| Role title | "Ingest conductor (voice + feeds + signals)" | Plain English, matches remit, avoids corporate-speak ("orchestrator"). |
| Emoji | 🎙️ microphone | Voice memos are the primary Wave 4 driver; the mic reads as intake/listening. Considered 🌊 (wave/Levant) but less intuitive. |
| Accent color | Cyan `#06b6d4` (Tailwind cyan-500) | Avoids collisions: Cordis indigo `#6366F1`, Tokamak slate `#475569`, Corva purple `#a855f7`, Hild coral `#f97316`, Axel green `#22c55e`, old-Lev amber `#f59e0b`, Kardia rose `#f43f5e`, Feynman teal `#14b8a6`. Cyan sits cleanly between teal and indigo in the existing palette, reads as "signal / wave / intake." |
| Name provenance | "Lev" short for *Levant* (rising side, where things come in) | Edmund-flavored, matches the Cordis-style Latin/Greek root gloss. |

## Tool allowlist (documented in CLAUDE.md)

- **Allowed:** `mcp__capture-mcp__capture`, Supabase MCP (`execute_sql` read + scoped writes to `observations` / `signal_source_health`), Edge Function invocation (`ingest-youtube`, `signals-ingest`, future `ingest-voice` / `ingest-pdf`), WORKSPACE_TOOLS (create/list/get), COMMUNICATION_TOOLS (slack + task inbox), FILE_TOOLS in local dev (list/read/move/rename/create-dir/write), web search / fetch.
- **Explicitly excluded:** `delete_file`, `apply_migration`, `deploy_edge_function`, Pinecone MCP writes, Notion MCP writes, any `skill_versions` / `reference_docs` mutation, any flip of `observations.approved_at`.

Rationale: mirrors the Cordis pattern (capture-first, writes stay in the low-trust lane) and adds the ingest-specific edge-function calls. `delete_file` excluded per W4.1 constraint.

## Verification

Pure content creation — no code to run, no migrations, no services to ping. Manual review covers the checklist:

- [x] `identity.md` has Name, Role, Emoji, Accent Color fields (parseable by `parseIdentity()` in `dashboard/lib/agents.ts`).
- [x] `CLAUDE.md` has a system prompt + routing table with "Route when Edmund mentions ... | keywords" shape.
- [x] Accent color `#06b6d4` does not collide with any existing agent's color.
- [x] Total length under 400-line budget (~160 lines combined).
- [x] No other agent files touched.
- [x] No runtime registration change; dashboard loads agents from disk at request time per `dashboard/CLAUDE.md` "Agent Definition" section.
- [x] Backlog W4.1 flipped to 🟢 with one-liner.

## Decisions made this run

1. **Put Lev in `/factory/agents/` not in cowork.** Backlog says `/agents/lev/`; `/factory/agents/cordis/` is the established rebuild pattern. Cowork's older "Lev" (marketing) is out of scope and untouched.
2. **No `soul.md`.** `finished-state.md` doesn't require it, and Cordis' `soul.md` (cowork-side) isn't load-bearing for the MVP loops. Skip v1; add later if voice drifts.
3. **Two-phase ingest called out as the house pattern.** Both shipped Edge Functions (`ingest-youtube` W4.4, `signals-ingest` W4.5) use it; Lev's prompt codifies it so future `ingest-pdf` / `ingest-voice` match.
4. **Best-effort embedding codified.** Matches the actual behavior of `capture()` v6 (returns `memory_warning` if OpenAI key missing) and `signals-ingest` (no score gate).

## Follow-ups flagged

- **Older "Lev" in cowork/marketing.** Edmund may want to rename (e.g. to "Cora" / "Brand" / retire) since the rebuild Lev now owns the name. Low priority — the rebuild doesn't load the cowork file for Lev.
- **Dashboard agent list.** If Edmund surfaces `/factory/agents/` in the dashboard agent directory, we'll want a `getAgents()` codepath that reads this tree. Currently `dashboard/lib/agents.ts` reads from `COWORK_PATH/Agent Personalities/agents/`. Either symlink or add a second source; batch with W3.x dashboard work.
- **W4.2 voice-memo capture path** is the logical next epic — Lev now has a system prompt assuming that pipe exists. Priority bumps on the `⚪` queue.
- **Soul file.** If Edmund sees Lev drift off-voice in practice, we add `soul.md` — don't pre-emptively.

## Cost

Negligible. Pure file authorship. No LLM, no API, no migrations.

## What's next

Wave 4 unblocks incrementally now that Lev has a prompt:

- **W4.2** — Voice-memo capture path in `capture()` (audio → Storage → Whisper → transcript → text capture). Was waiting on Lev's prompt to orient the transcription call site; now unblocked.
- **W4.3** — iPhone Shortcut template for the webhook (depends on W4.2).
- W4.4 / W4.5 are already shipped; Lev now has a system prompt that knows they exist.

Proceeding to W4.2 unless Edmund redirects.

---

## Postscript — 2026-04-17 move to cowork tree

Edmund picked option (a): consolidate agent folders to `COWORK_PATH/Agent Personalities/agents/` so the dashboard agent loader picks them up.

Lev's files moved:
- **From:** `/Users/edmundmitchell/factory/agents/lev/{identity.md,CLAUDE.md}`
- **To:** `COWORK_PATH/Agent Personalities/agents/lev/{identity.md,CLAUDE.md}`

Content unchanged; only location moved. The dashboard will pick Lev up automatically on the next page load.

Follow-up still open: the `marketing/` folder contains an older "Lev" persona (amber/brand-strategist) at `COWORK_PATH/Agent Personalities/agents/marketing/identity.md` (and `CLAUDE.md`). Not touched in this move — separate cleanup decision for Edmund.
