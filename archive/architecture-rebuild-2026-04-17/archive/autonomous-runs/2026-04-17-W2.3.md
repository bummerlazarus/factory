# Autonomous run log — W2.3

**Epic:** W2.3 — Webhook doc (one URL, many callers)
**Plan:** inline (doc-only)
**Status:** 🟢 DONE
**Done:** 2026-04-17

## Summary

Published `05-design/capture-api.md` — the canonical reference for calling the capture pipeline from every surface. No code changes; the Edge Function URL is already the public webhook (deployed v7, `verify_jwt: false`, shared-secret auth).

## Files touched

- `architecture-rebuild-2026-04-17/05-design/capture-api.md` — new. Sections: one URL, headers, body schema, response shape, where data lands, entry-point recipes (Claude chat, dashboard, iPhone Shortcut, URL bookmarklet, email-forward via Zapier/Make, scheduled Claude tasks), secrets setup, rate-limit TODO, idempotency, errors.

## Verification

Doc-only epic. Content accuracy verified against:
- Live `capture` deployment — v7 confirmed via `list_edge_functions` and `get_edge_function` (see W2.1b/W2.1c run logs).
- `CAPTURE_SECRET` status — verified set via `HTTP 401 unauthorized` response on a missing-secret POST.
- `OPENAI_API_KEY` + `FIRECRAWL_API_KEY` status — unknown (not queryable via MCP); doc documents the "unknown / pending" state and what a successful set looks like.

## Decisions made

- **Recipe shape per entry point.** Each recipe is a self-contained block — user sees exactly what to set up on their side. No "see the OTHER doc" hops.
- **Bookmarklet inline.** Included despite the caveat about embedding secrets in browser bookmarks; convenience wins for a single-user tool if Edmund wants it. Doc notes the leak risk.
- **Rate limiting = TODO, not blocker.** Until the webhook surface expands beyond Edmund's own tools, the shared secret is enough. When external consumers are added, flag for an edge rate-limit layer.
- **Idempotency note.** Documented current partial-unique-index protection and the content-hash enhancement path; didn't build it.

## Follow-ups flagged

- **MCP tool `capture` (W2.4).** Recipe #1 (Claude chat) shows the `curl` shell-out today. Once W2.4 lands, update recipe #1 to reference the MCP tool instead.
- **Voice memo recipe.** Placeholder in recipe #3 for dictation-via-Shortcut; full voice-memo path = W4.2 (audio upload + transcription).
- **Secrets status banner.** The doc has a status table noting which secrets are verified set vs. unknown. When Edmund confirms, flip to ✅ in that section.

## Cost

None.

## What's next

Move to W2.2 — dashboard `/inbox` page with Realtime on `work_log`.
