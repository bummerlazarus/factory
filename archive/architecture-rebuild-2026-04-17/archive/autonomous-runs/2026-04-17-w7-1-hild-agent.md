# Run log — W7.1 Hild agent scaffold

**Date:** 2026-04-17
**Epic:** W7.1 — Hild agent: system prompt + tool allowlist. Scope: DC client work (CFCS, Liv Harrison, Culture Project, Lisa) + ZPM/Real+True ops.
**Status:** 🟢 DONE
**Pattern reference:** Axel W5.1 (personality-named folder next to `cordis/` in COWORK_PATH) and Lev W4.1 (tool-allowlist + routing-table shape).

## Files written

| File | Purpose | Lines |
|---|---|---|
| `/Users/edmundmitchell/Library/Mobile Documents/com~apple~CloudDocs/CEO Cowork/Agent Personalities/agents/hild/identity.md` | Name, role, emoji, accent color; character; primary remit; speaking style; what Hild is NOT | 55 |
| `/Users/edmundmitchell/Library/Mobile Documents/com~apple~CloudDocs/CEO Cowork/Agent Personalities/agents/hild/CLAUDE.md` | System prompt: who-she-serves, core directives, projects-she-owns (DC clients + ZPM + Real+True), tool allowlist (scoped tight), workflow, hand-off rules, routing table | 144 |
| `/Users/edmundmitchell/factory/architecture-rebuild-2026-04-17/06-handoffs/backlog.md` | Flip W7.1 ⚪ → 🟢 with one-line note | 1-line edit |

Total agent content: **199 lines** — well under the <400 line budget.

No `soul.md` — per brief, v1 skips it.

## Folder choice + one-line reason

`COWORK_PATH/Agent Personalities/agents/hild/` — matches the personality-named-folder pattern Axel (W5.1) just established next to `cordis/`, and matches the brief's explicit "almost certainly" hint. Name-clash note below.

### Name-clash with legacy `designer/` folder

`COWORK_PATH/Agent Personalities/agents/designer/identity.md` already has `**Name:** Hild` — the legacy visual-design agent. Two "Hild" agents now coexist:

- **Legacy (`agents/designer/`)** — Visual design specialist & art director, 🎨 coral (#f97316). Untouched per constraint.
- **New (`agents/hild/`)** — Client + Ops Steward, 🧭 emerald (#059669).

Dashboard loads by folder ID, so these are two distinct agents in the roster even though they share a display name. Cordis's own `CLAUDE.md` still routes design keywords (thumbnail, banner, carousel, visual, branding, Canva, Figma, asset) to `agents/designer/` — untouched.

**Edmund's call post-hoc:** Rename the legacy designer agent to something else (the visual-design remit is no longer owned by "Hild" in the rebuild), retire it, or leave the duplicate name and accept the drift. Not blocking. The new `hild/CLAUDE.md` hand-off table explicitly calls out the collision and tells the agent to flag it if it's actually ambiguous in a session.

## Emoji + color picked + one-line reason

### Emoji: 🧭 compass

- Reads as stewardship / navigation / "keeps the ship on course" — maps to Hild's obligation-tracking remit.
- No collision: existing set is 🤖 (Cordis), 🛠️ (Axel-new), 🎙️ (Lev), 🎨 (Hild-designer-legacy), 📊 (Tokamak/CEO), ✍️ (Corva/content), 📣 (Lev-marketing-legacy), 🔍 (Feynman/research), 🗂️ (Kardia/PM), ⚙️ (Axel-developer-legacy). 🧭 is visibly distinct.
- Considered 🪴 (stewardship/care), 🌿 (craft), 🕯️ (vigil/watching), 🗝️ (promise-keeping). Compass wins for "steward with a sense of direction" energy.

### Accent Color: Emerald `#059669` (Tailwind emerald-600)

- Reads as steady, grounded, evergreen — stewardship energy.
- No collision: Cordis indigo `#6366f1`, Axel-new sky `#0ea5e9`, Axel-dev-legacy green `#22c55e`, Lev-new cyan `#06b6d4`, Hild-designer-legacy coral `#f97316`, Corva purple `#a855f7`, Feynman teal `#14b8a6`, Tokamak slate `#475569`, Lev-marketing-legacy amber `#f59e0b`, Kardia rose `#f43f5e`.
- Emerald-600 (`#059669`) sits cleanly between green-500 and teal-500 on the Tailwind ring; visibly darker/cooler than either. Also distinct from the `#10b981` emerald-500 that appears in `ceo/soul.md` (different shade, and that one is in a soul file not an identity file so it doesn't claim a dashboard identity slot).

## Tool allowlist (documented in CLAUDE.md)

**Allowed:**
- `mcp__capture-mcp__capture` (primary write path; every client update flows through it with `project` tag + `artifacts.client` for DC work).
- Supabase MCP: `execute_sql` reads against `sessions`/`work_log`/`observations`/`reference_docs`/`signals`/`memory`; scoped writes to `observations` (low-confidence drift only), own `sessions` row, own-session `work_log.artifacts` updates.
- Workspace tools (`create/list/get/update_workspace_item`) scoped to `department=strategy` or `project IN ('zpm','real-true','dc-clients')`.
- Slack-style messaging + task inbox.
- `reference_docs` **read-only** by project tag.
- Notion MCP reads (Work DB + client pages); web search / fetch / Firecrawl for client verification.

**Explicitly excluded:**
- `deploy_edge_function`, `apply_migration`, Supabase branch mutations (Axel territory).
- Publishing tools (`publish_to_beehiiv`, `publish_instagram_*`, `publish_to_vault`) — Corva territory.
- Any `skill_versions` INSERT/UPDATE (Corva + Edmund approval loop only).
- Any mutation to `reference_docs` (read-only).
- `delete_file`, filesystem deletes.
- Drafting Edmund's personal-brand content (Axel).
- Designing visuals (legacy `designer/` agent).

Rationale: mirrors the Cordis/Lev/Axel pattern — capture-first, writes stay in the low-trust lane. Ops-specific scoping added: `workspace` tools restricted to the project slugs Hild actually owns so she can't create a plan under `factory` or `em-brand` by accident.

## Projects-owned scoping (taste call Edmund should review)

The CLAUDE.md explicitly enumerates which `project` tags Hild owns vs routes:

- **Owns:** `zpm`, `real-true`, `dc-clients` (with `artifacts.client ∈ {cfcs, liv-harrison, culture-project, lisa}`).
- **Routes:** `em-brand`, `cordial-catholics`, `faith-ai`, `factory`, `other`.

Faith & AI Project is a judgment call — per `project-statuses.md` it has an ops dimension (DELTA grant paperwork, Pushpay collab, Gerardo follow-up) that could fit Hild, but the primary thesis + content work lives elsewhere. I made Hild route FAI by default and touch it only when an ops item explicitly lands. Edmund: push back if FAI should be a Hild-owned project.

Lisa is in the client roster per `project-statuses.md` ("New client, 2x/month consulting cadence") but isn't in the backlog's W7.1 scope bullet. Included in Hild's routing on the assumption that "DC client work" is the real remit and Lisa fits that class. Easy to remove if not.

## Verification

Pure content creation. Manual review covers the checklist:

- [x] `identity.md` has Name, Role, Emoji, Accent Color fields (parseable by `parseIdentity()` in `dashboard/lib/agents.ts`).
- [x] `CLAUDE.md` has system prompt + routing table with "Route when Edmund mentions ... | keywords" shape.
- [x] Emoji 🧭 does not collide with any existing agent's emoji.
- [x] Accent color `#059669` does not collide with any other agent's identity color.
- [x] Total length 199 lines — under the 400-line budget.
- [x] No other agents' files touched (legacy `designer/` untouched despite Name clash).
- [x] No runtime registration change; dashboard loads from disk.
- [x] Backlog W7.1 flipped to 🟢.
- [x] Cited real file paths for client context (`project-statuses.md`) instead of inventing client-specific content.

## Decisions made this run

1. **COWORK_PATH over `/factory/agents/`.** Axel (W5.1, most recent sibling) placed files in COWORK_PATH. Lev (W4.1) placed in `/factory/agents/`. The two-location split is already a known follow-up; adding Hild to COWORK_PATH keeps the newer convention consistent. Also matches the brief's "almost certainly `COWORK_PATH/Agent Personalities/agents/hild/`" hint.
2. **Accept the name clash with legacy `designer/` (also named Hild).** Per brief constraint "Don't touch other agents' files." Dashboard loads by folder, so the runtime works; Edmund resolves the display-name duplication post-hoc.
3. **Project-scope enumeration inline.** Written out which `project` tags Hild owns vs routes, so the agent doesn't fuzzy-match captures into the wrong department.
4. **`reference_docs` reads only, no writes.** Client scopes and brand guides are authored by Edmund or by a specialist (Axel writing a repurpose template, e.g.) — not by Hild. Hild consults; she doesn't author durable reference.
5. **Drift-observation cadence documented.** Weekly last-touched sweep is specified with an actual SQL shape and an `observations` row format, so it's runnable the moment the agent comes online.

## Ambiguity Edmund should resolve

1. **Legacy `designer/` agent name.** Two "Hild" agents in the roster. Options: (a) rename legacy to "Aurelia" / "Cora" / something-else, (b) retire it entirely (the rebuild's six-agent troupe doesn't list a dedicated visual-design agent), (c) leave both and accept drift. Not blocking W7.1.
2. **FAI ownership.** `project-statuses.md` has FAI ops items (DELTA grant, Pushpay, Gerardo). Currently routed away from Hild. Flip on request.
3. **`project=dc-clients` vs per-client project slugs.** Currently Hild uses a single `project=dc-clients` tag with `artifacts.client=<slug>`. Alternative: split into `project=cfcs` / `project=liv-harrison` / `project=culture-project` / `project=lisa`. The closed-set tag list (in Cordis's `CLAUDE.md`) uses `dc-clients`, so I matched that. If Edmund wants per-client projects, it's a `work_log` schema + Cordis prompt change — do together.
4. **Lisa in scope.** Included in Hild's routing ("DC client work" covers her). Remove if Lisa is a separate track.

## Follow-ups flagged

- **W7.2 client project scaffolding.** Hild has a prompt that assumes per-client workspace shape + `work_log.project` tags; W7.2 is the concrete implementation of that convention. Unblocked by this run.
- **Project-statuses.md is canonical context.** Keep it updated; Hild's session-open step reads it. Stale doc → stale Hild reports.
- **Legacy designer rename/retire.** Low priority — the rebuild doesn't load both for the same domain anywhere.
- **No `soul.md`.** If Hild drifts off-voice in practice, add later — don't pre-emptively.

## Cost

Negligible. Pure file authorship. No LLM, no API, no migrations, no services.

## What's next

Wave 7 unblocks incrementally:

- **W7.2** — Client project scaffolding in the dashboard; `work_log.project` tags enforced. Hild now has a prompt that assumes this shape.
- Downstream W8 specialist-spawn workflow can reference Hild as the parent role when DC client volume justifies a dedicated subcontract-ops specialist.

Proceeding to whichever epic Edmund picks next.
