# W10.10 — UI wiring for the compression engine

**Date:** 2026-04-19
**Author:** autonomous orchestrator (per autonomy-charter)
**Backlog:** `06-handoffs/backlog.md` W10.10
**Context:** W10 compression engine shipped in `a519da3`. Chat tools + initial UI wiring shipped in `31a9454`. This plan closes the remaining gaps.

## Audit: what's already on `feat/compression-engine`

Read `git diff main..feat/compression-engine --stat` and the relevant source files.

| Scope item | Status |
|---|---|
| Chat tools for Axum/Sophia/Kontra/Kairos/Augustin | ✅ Done — `lib/tools.ts` defines all 5, `lib/tool-tags.ts` filters per agent, agent-runner wires them in. |
| `/research` kinds extension (tension / contradiction / permanent-ip) | ⚠️ Partial — list filters include `tension` + `research-brief`. Missing: `atomic-draft` (surfaced on `/compression` but not `/research`), and a Kairos-staged filter (the "permanent-ip" concept). Scope's "contradiction" maps to live kind `tension`. Scope's "permanent-ip" is not a DB kind; per decisions-log W10 entry, Kairos stamps `metadata.gate_status` on any kind — the filter is semantic. |
| `/research/[id]` detail route | ✅ Done — renders body markdown, sources, reviewer votes, debate blocks. |
| `/compression` Approve/Dismiss surface | ⚠️ UI done, BACKEND BROKEN. `/compression` surfaces `atomic-draft`, `tension`, `research-brief` rows with the same `<ResearchActions>` component that POSTs to `/api/research/decide`. That route rejects every kind except `theme`/`connection` (`route.ts:46`). **Every Approve/Dismiss click from `/compression` currently returns HTTP 400.** |
| Sidebar nav entry `/compression` | ✅ Done — `components/layout/sidebar.tsx`. |
| Ask Sophia button | ✅ Done — `app/research/ask-sophia-button.tsx`, mounted in `/research` list. |

## Gap list (what this plan closes)

### G1 — Unblock Approve/Dismiss for compression-engine kinds (P0, broken UX)

`app/api/research/decide/route.ts` whitelist:
- Currently: `theme`, `connection`
- Needs to accept: `theme`, `connection`, `tension`, `atomic-draft`, `research-brief`, `ip-map`

**Acceptance:** POST `/api/research/decide` with `{id, action:"approve"}` on a `tension` row returns 200 and the row's `metadata.approved_at` is set. Same for `atomic-draft` + `research-brief`. Reject unknown kinds with the existing 400.

**Verification:** start dev server via `preview_*`, navigate to `/compression`, click Approve on a card, confirm the card disappears on refresh and that `reference_docs.metadata->>approved_at` is populated (verify via Supabase SQL).

### G2 — `/research` lists all compression kinds + Kairos filter (P1)

`app/research/page.tsx`:
- Extend `VALID_KINDS` to include `atomic-draft` (already listed for tension/research-brief).
- Add a "Kairos-staged" filter chip that matches `metadata.gate_status = 'staged'`. Same Approve/Dismiss behavior — no new endpoint needed. Treated as a view filter, not a kind.

**Acceptance:** Filter chips on `/research` are: All · theme · connection · tension · research-brief · atomic-draft · staged. Clicking "staged" shows only rows where `metadata->>gate_status = 'staged'` regardless of kind.

**Verification:** `preview_*`, click each chip, confirm URL updates (`?kind=X` or `?staged=1`) and list re-filters.

### G3 — Detail-page badge coverage (P2, polish)

`app/research/[id]/page.tsx` `kindBadgeClass` already covers all 5 relevant kinds. Add `ip-map` (already used elsewhere) for completeness. No-op for kinds unlikely to reach detail.

**Acceptance:** Detail page for an `ip-map` row renders with a styled badge, not the default muted.

## Branching

`feat/compression-engine` is 21 commits ahead of main and includes unrelated work (inbox file upload, chat streaming fixes). W10.10 should land cleanly; cherry-picking would be more work than just adding to this branch. **Decision: continue on `feat/compression-engine`.** One focused commit per gap.

## DB work

None. `reference_docs_kinds` already includes `tension`, `atomic-draft`, `research-brief` (verified via live Supabase MCP query). No new kinds needed — "contradiction" and "permanent-ip" per the backlog are semantic aliases, not DB values.

## Worktree

Per charter safety-net: use `superpowers:using-git-worktrees`. Worktree name: `w10-10-ui-wiring`. Base: `feat/compression-engine`.

## Verification plan

1. `pnpm typecheck` (or the project equivalent) clean.
2. `preview_start` dev server.
3. Navigate `/compression` → Approve a tension row → confirm row disappears, confirm `preview_network` shows `POST /api/research/decide` → 200.
4. SQL check: `SELECT id, kind, metadata->>'approved_at' FROM reference_docs WHERE id = '<id>'` — timestamp present.
5. Navigate `/research` → click each filter chip → confirm filter behavior.
6. Open a detail row from `/research` → confirm page renders.
7. Screenshot all three surfaces.

## Out of scope

- No change to the Approve/Dismiss semantics of `theme`/`connection` (still stamps `approved_at` / `dismissed_at`).
- No new kinds in `reference_docs_kinds`.
- No change to edge functions.
- No merge to main (charter hard-stop).
