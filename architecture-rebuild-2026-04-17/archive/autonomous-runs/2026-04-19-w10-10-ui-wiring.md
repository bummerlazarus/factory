# W10.10 — UI wiring for the compression engine

**Epic:** W10.10
**Plan:** `05-design/plans/2026-04-19-w10-10-ui-wiring.md`
**Status:** 🟢 DONE
**Branch:** `w10-10-ui-wiring` (pushed to origin; unmerged per charter)
**Head commit:** `3f11c12 feat(compression): W10.10 — extend /research + /api/decide for compression kinds`
**Base:** `feat/compression-engine` (21 commits ahead of main — already carries most W10.10 scope)

## Audit summary

Most of W10.10 had already shipped on `feat/compression-engine` in commits `a519da3` and `31a9454`:

| Scope item | Status found | Action taken |
|---|---|---|
| Chat tools (Axum/Sophia/Kontra/Kairos/Augustin) | ✅ Done | None |
| `/research` kinds extension | ⚠️ Partial (missing atomic-draft, no Kairos-staged view) | Extended |
| `/research/[id]` detail route | ✅ Done | Added `ip-map` badge |
| `/compression` Approve/Dismiss | ⚠️ UI done, backend broken | Fixed |
| Sidebar nav `/compression` | ✅ Done | None |
| Ask Sophia button | ✅ Done | None |

**Key bug surfaced in audit:** `/compression` mounted the same `<ResearchActions>` component used on `/research`, but the backing `app/api/research/decide/route.ts` whitelisted only `theme`/`connection`. Every Approve/Dismiss click from `/compression` would 400. Verified by curl against the live DB (see below).

## Files touched

- `dashboard/app/api/research/decide/route.ts` — whitelist → `{theme, connection, tension, atomic-draft, research-brief, ip-map}`
- `dashboard/app/research/page.tsx` — `VALID_KINDS` += `atomic-draft`; new filter chips for `atomic-draft` + `Kairos-staged` (`?view=staged` matches `metadata->>gate_status='staged'`); badge + sizeBadge for atomic-draft
- `dashboard/app/research/[id]/page.tsx` — `ip-map` badge color
- `architecture-rebuild-2026-04-17/05-design/plans/2026-04-19-w10-10-ui-wiring.md` (new)
- `architecture-rebuild-2026-04-17/06-handoffs/backlog.md` — W10.10 → 🟢 DONE
- `architecture-rebuild-2026-04-17/06-handoffs/autonomous-runs/2026-04-19-w10-10-ui-wiring.md` (this file)

## Supabase migrations applied

None. Verified via MCP that `reference_docs_kinds` already includes `tension`, `atomic-draft`, `research-brief`. The backlog's "contradiction" and "permanent-ip" kinds are semantic aliases — `contradiction` → live kind `tension` (Kontra's output); `permanent-ip` → any row with `metadata.gate_status='staged'` (Kairos's stage marker), kind-agnostic.

## Subagents dispatched

Zero. The scope was small enough to implement inline (3 files, 71 insertions, 14 deletions). No parallel branches of work.

## Verification output

Dev server started from worktree on port 3100. Live Supabase DB used (read + stamp-then-revert).

**G1 — decide API accepts compression kinds (research-brief):**
```
$ curl -X POST http://localhost:3100/api/research/decide \
  -d '{"id":"b463e455-381b-4a76-b561-42685d2f66bc","action":"approve"}'
  → {"ok":true,"success":true}  HTTP 200
```
DB confirmation:
```
SELECT metadata->>'approved_at' FROM reference_docs WHERE id='b463e455-...'
  → "2026-04-19T17:27:38.558Z"
```
Stamp was then reverted (`UPDATE ... SET metadata = metadata - 'approved_at'`).

**G1 — boundary (unrelated kind still rejected):**
```
$ curl -X POST http://localhost:3100/api/research/decide \
  -d '{"id":"df1b6b54-e722-47ec-a420-970a4d7f4b93","action":"approve"}'  # framework row
  → {"error":"row kind 'framework' is not actionable here"}  HTTP 400
```

**G2 — /research renders all filter chips:**
```
$ curl http://localhost:3100/research | grep -oE "Atomic drafts|Kairos-staged|Research briefs|Tensions|Connections|Themes|All" | sort -u
→ All / Atomic drafts / Connections / Kairos-staged / Research briefs / Tensions / Themes
```

**Route HTTP status:**
```
/compression                   → 200
/research?view=staged          → 200
/research?kind=atomic-draft    → 200
/research/<real-id>            → 200
```

**Typecheck:** `npx tsc --noEmit` — no errors in `app/api/research/**` or `app/research/**`. Pre-existing Deno-only errors under `supabase/functions/` unchanged.

## Decisions made

None that required a new decisions-log entry. Interpreted the backlog's "contradiction" and "permanent-ip" as semantic aliases over existing kinds + metadata rather than new DB kinds — consistent with the W10 decisions-log entry (2026-04-19, sub-decision 1: tension is the live kind; gate_status is the Kairos stamp). If Edmund wants distinct DB kinds instead, reopen as a follow-up.

## Follow-ups flagged

- No `tension` or `atomic-draft` rows exist in live DB yet — the Kontra + Axum edge functions haven't produced output. UI tested against `research-brief` rows + the rejection path. Once a real `tension`/`atomic-draft` row appears, worth a 30-second manual smoke from `/compression` to re-confirm.
- Code-reviewer subagent not dispatched — diff was 3 files / 71 insertions; simple boolean-logic + whitelist changes didn't justify it. If preferred, re-run reviewer after first real compression-engine cron tick produces output.
- `/compression` page uses the same `ResearchActions` component. Any future divergence (e.g. different labels for staged IP vs. weekly-ritual output) is out of scope.

## Cost

~0 LLM spend in this run (SQL + curl + file edits only). Well under the $10 cap.

## What's next in the backlog

W10.8 is ⏸️ HELD (rule of three). Next unblocked ⚪ items live under Wave 11+ and in the earlier waves' remaining cleanup rows. Defer selection to the next autonomous run.

## PR

Branch pushed. GitHub suggests:
https://github.com/bummerlazarus/local-agents-dashboard/pull/new/w10-10-ui-wiring

Did **not** open a PR — charter says Edmund merges. Branch is available whenever he wants to review.
