# Run log — W5.2 IOC system import

**Date:** 2026-04-17
**Backlog:** W5.2
**Outcome:** 🟢 Imported

## Summary

Found the canonical IOC directive in `reference/archive/ioc-system-v2/`, picked `IOC_DIRECTIVE.md` as THE spec, inserted one row into `public.reference_docs`, wrote the plan, flipped W5.2 in the backlog.

## Steps

1. **Recon.** Searched three likely trees:
   - `/Users/edmundmitchell/factory/reference/archive/` — hit: `ioc-system-v2/`
   - `/Users/edmundmitchell/Library/Mobile Documents/com~apple~CloudDocs/CEO Cowork/` — only downstream mentions (Axel files, EM brand docs)
   - `/Users/edmundmitchell/factory/architecture-rebuild-2026-04-17/` — only backlog/audit references, no fresh spec
   - Note: task prompt mentioned `/factory/reference/reference-repos/gravityclaw/` — that path doesn't exist under `reference/`; the actual archive is `reference/archive/ioc-system-v2/`.

2. **Pick.** Compared archive contents (`ARCHIVE_NOTE.md`, `IOC_DIRECTIVE.md`, `IOC_System_v2.0_FileIndex.md`). Picked `IOC_DIRECTIVE.md` — the archive itself calls it the agent entry point with terminology + schema. Richer than the index, more canonical than the audit.

3. **Schema check.**
   ```sql
   SELECT column_name, data_type FROM information_schema.columns
   WHERE table_schema='public' AND table_name='reference_docs';
   -- columns: id, slug, title, body, kind, version, metadata, updated_at, created_at
   ```
   Task description had `content` / `project` which don't exist; the real column is `body`, and project lives inside `metadata`. Adjusted and proceeded without altering the table.

4. **Confirmed empty table.** `SELECT ... FROM public.reference_docs` → 0 rows. Clean first-row import.

5. **Insert.** Via Supabase MCP `execute_sql`. Used a `$BODY$…$BODY$` dollar-quoted literal for the 250-line markdown body so no escaping tax.
   - Row id: `df1b6b54-e722-47ec-a420-970a4d7f4b93`
   - slug: `ioc-system-v2`
   - kind: `framework`
   - version: `1`
   - body_len: `10804`
   - metadata: `{source_path, source_version, source_date, imported_at, imported_by, project, related_candidates, notes}`

6. **Memory embedding — deferred.** No OpenAI key available locally to this session; embeddings in this stack are done by an Edge Function with function-secret `OPENAI_API_KEY`. Flagged in the plan doc as a follow-up rather than forcing it here.

7. **Docs.** Wrote `05-design/plans/2026-04-17-w5-2-ioc-import.md` and this run log.

8. **Backlog.** Flipped W5.2 to 🟢 with a one-line shipped summary.

## Deliverables

- `public.reference_docs` row `df1b6b54-e722-47ec-a420-970a4d7f4b93` — slug `ioc-system-v2`
- Plan: `05-design/plans/2026-04-17-w5-2-ioc-import.md`
- Run log: this file
- Backlog update: W5.2 → 🟢

## Follow-ups flagged

- Kick off embedding of the row into `public.memory` (namespace `knowledge`, source `reference_doc`, source_id `df1b6b54-e722-47ec-a420-970a4d7f4b93`) once Edmund wants semantic retrieval over it.
- Possible new backlog item: decide whether any of the 40 A/N/E/B/O/S directive SOPs should also land in `reference_docs` or `skill_versions`.
